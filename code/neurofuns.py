## neurofuns

def peakTriggeredAvg(signal_data: np.array, threshold: float):
    """
    This code takes a signal and some threshold, then computs a peak triggered average.
    
    The peak triggered averaged is estimated by finding the times corresponding to a peak event,
    taking time around that event, storing it in a matrix of events (row = events, col = signal),
    then conducting the average across events
    """


    pass
